{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Please deploy the frontend or backend service directly'"
  },
  "deploy": {
    "startCommand": "echo 'Please deploy the frontend or backend service directly'"
  }
}
